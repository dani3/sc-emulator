{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build sc-emulator",
            "command": "/usr/bin/gcc",
            "args": [
                "-g",
                "-o",
                "sc-emulator",
                "./src/main.c"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}